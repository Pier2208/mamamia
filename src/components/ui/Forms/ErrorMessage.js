import styled from 'styled-components'

const ErrorMessage = styled.span`
    width: fit-content;
    font-size: .8rem;
    color: #DF2935;
    margin-top: .3rem;
    position: absolute;
    bottom: -20px;
    left: 5px;
`
export default ErrorMessage